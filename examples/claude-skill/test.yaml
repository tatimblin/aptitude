name: "Cloud status skill activation"
prompt: "/cloud-status"

assertions:
  - tool: execute_command
    called: true
    params:
      command: "python3*check_status.py*"

  - tool: web_fetch
    called: false
    params:
      url: "*amazon.com/*"
